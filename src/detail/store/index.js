//将组件自己redux所有部分导出

import DetailReducer from "./reducer";
import * as actionTypes from "./actionTypes";
import * as actionCreators from "./actionCreators";
export { DetailReducer, actionTypes, actionCreators };
